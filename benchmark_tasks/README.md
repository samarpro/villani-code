# Benchmark Tasks

This directory stores objective, reproducible benchmark tasks used by `villani-code benchmark`.

- Each task is a JSON file validated by `BenchmarkTask`.
- Tasks should include deterministic validation checks.
- These starter tasks focus on Villani Code UI/runtime regressions.
