id: bugfix_013_header_propagation
family: bugfix
difficulty: medium
language: python
max_minutes: 3
max_files_touched: 4
expected_artifacts:
- patch
visible_verification:
- pytest -q
hidden_verification:
- cd repo && PYTHONPATH=src python -c "from app.core import normalize_path; p=normalize_path(r'svc\\api\\v1'); assert p.startswith('svc/'); assert p.endswith('/v1')"
success_policy:
  require_visible_pass: true
  require_hidden_pass: true
  fail_on_timeout: true
  fail_on_repo_dirty_outside_allowlist: true
allowlist_paths:
- src/
- tests/
benchmark_track: core
