{
  "id": "wrap_no_horizontal_scroll",
  "name": "UI wraps without horizontal scroll",
  "repo_path": ".",
  "instruction": "Fix UI text wrapping so long content wraps without requiring horizontal scrolling, and validate through focused tests.",
  "category": "ui-layout",
  "tags": ["ui", "layout", "wrapping"],
  "timeout_seconds": 300,
  "expected_touched_paths": [
    "villani_code/tui/styles.tcss",
    "tests/test_live_display.py"
  ],
  "forbidden_touched_paths": [
    "villani_code/openai_client.py"
  ],
  "validation_checks": [
    {
      "type": "command",
      "command": "pytest tests/test_live_display.py -q",
      "expect_exit_code": 0
    }
  ],
  "notes": "Ensures line wrapping behavior remains stable."
}
